import React, {useState} from 'react';
import {StyleSheet, View, Text} from 'react-native';

const Grow = ({navigation}) => {
  return (
    <View>
      <Text>새싹키우기</Text>
    </View>
  );
};

export default Grow;
