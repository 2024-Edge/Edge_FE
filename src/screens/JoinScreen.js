import React, {useState} from 'react';
import {StyleSheet, View, Text} from 'react-native';

const Join = ({navigation}) => {
  return (
    <View>
      <Text>로그인</Text>
    </View>
  );
};

export default Join;
